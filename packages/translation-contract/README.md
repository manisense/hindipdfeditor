# Translation contract

Runtime-validated request and response types shared by the Android app, web app, and Cloudflare
Worker. The package intentionally has no runtime dependencies so each surface uses the same limits,
directions, status codes, and line-ID validation.
