# Cloudflare Pages static site for hindipdfeditor.com
name = "hindipdfeditor"
compatibility_date = "2026-07-07"

# Used by `wrangler pages deploy` and Pages Git builds
pages_build_output_dir = "dist"
